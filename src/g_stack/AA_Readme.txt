Identification :

When There is an array
During broute force In Array we can solve problem using two loops, complexity :O(n^2)
Outer loop i and inner loop j
    inner loop j is dependent on i
        j = i -> n-1
        j = n-1 -> i
        j = 0 -> i
        j = i -> 0


100 % this problem can be solved by stack.